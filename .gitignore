# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/app/
/build/ios/
/build/linux/
/build/macos/
/build/windows/
/build/android/
/build/native_assets/
/build/test_cache/
/build/unit_test_assets/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.vercel
.env*.local
node_modules

# Signing keys — never commit the Android/iOS keystore (hasn't been
# committed so far, but added preemptively to keep it that way)
android/key.properties
*.jks
*.keystore
*.p12
*.mobileprovision

# Claude Code project instructions (local only)
CLAUDE.md

# Supabase CLI local cache
supabase/.temp/
